# virtual environments helps us to
# have more versions of all these packages on the same machine

# pipenv allows us to use PIP
# and this virtual env to install packages based on each project
# so each project has its own packages

# pycharm has default virtual env for each project
